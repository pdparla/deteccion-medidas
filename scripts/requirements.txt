# Core ML dependencies
tensorflow>=2.15.0
tensorflow-hub>=0.15.0
numpy>=1.24.0

# PyTorch (use CPU version for compatibility)
torch>=2.0.0
torchvision>=0.15.0

# Model conversion (Linux only - use devcontainer on Windows)
ai-edge-torch>=0.1.0
